{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "chrome",
            "request": "launch",
            "name": "Run SPHF Dashboard",
            "file": "${workspaceFolder}/index.html"
        }
    ]
}