{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "chrome",
            "request": "launch",
            "name": "Run SPHF Dashboard",
            "url": "http://localhost:8080/index.html",
            "webRoot": "${workspaceFolder}",
            "server": {
                "command": "python -m http.server 8080"
            }
        }
    ]
}